import { GITHUB_REPO, GITHUB_RESPONSE } from "./github_api.types";
import { CardRepoProps, CardProyectProps } from "./component.cards.types";
import { SearchEngineProps } from "./component.inputs.types";

export type {
  GITHUB_REPO,
  GITHUB_RESPONSE,
  CardRepoProps,
  CardProyectProps,
  SearchEngineProps,
};
